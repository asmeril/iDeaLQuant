commit e9d84ab9ca32bcaf09ced78239bb6d281eba2b5b
Author: asmeril <asmeril@gmail.com>
Date:   Sun Feb 22 17:00:09 2026 +0300

    docs: update devlog and roadmap with active_days bugfix resolution

diff --git a/DEVLOG.md b/DEVLOG.md
index 9dfcf6c..64c365e 100644
--- a/DEVLOG.md
+++ b/DEVLOG.md
@@ -6,13 +6,15 @@
 - **TypingError Fix (`times_arr`):** Tüm `IndicatorCache` (S1, S2, S3, S4) sınıflarında `df['date'].dt.date.values` kullanımı sebebiyle array'in Python objesi (PyObject) array'i olarak kalması ve Numba'nın (`@jit(nopython=True)`) bu array'i derleyemeyerek çökmesi engellendi.
   - **Çözüm:** Tüm tarih sütunları `values.astype(np.int64) // 10**9` ile saf `np.int64` tabanlı UNIX timestamp'e çevrildi. Kod C++ hızından taviz vermeden ve çökmeden çalışabilir hale geldi.
 - **Argüman Sayımı Doğrulamaları:** Tüm stratejilerin optimizer dosyaları içerisinde `fast_backtest_X` argüman eşleşmeleri (`fast_backtest_strategy4` için 19 argüman, S2 için 24 argüman vb.) baştan sona denetlendi ve tutarlılıkları test edildi.
+- **OOS Active Days Sayım Hatası Giderildi:** `IndicatorCache`'de saniyeye dönüşüm sırasında `df['datetime']` ile gelen objelerin Pandas 2.0 micro-seconds `[us]` çözünürlüğünde olması sebebiyle oluşan gün verisi kayıpları engellendi. 
+  - **S1, S2, S3, S4:** Tümü `astype('datetime64[s]')` kalıbıyla saniyeye zorlanarak sabitlendi. Yeni ceza puanlama sistemindeki `1 gün` ve saçma yoğunluk hesaplamaları düzeltildi.
 
 ### 📁 Değişen Dosyalar (4)
 `strategy1_optimizer.py`, `strategy2_optimizer.py`, `strategy3_optimizer.py`, `strategy4_optimizer.py`
 
 ### 📌 Mevcut Durum
-- **Aktif Faz:** Faz 6 - Desktop UI Testi & İyileştirme
-- **Sıradaki Adım:** Optimizer stabilite testleri (Hatasız tamamlanma kontrolü).
+- **Aktif Faz:** Faz 9 - Canlı Test & Robust Analizleri
+- **Sıradaki Adım:** Yeni OOS modelleri ile en sağlam S1, S2, S3 ve S4 setlerini belirleyip Live Trade verisine almak.
 
 ---
 
diff --git a/ROADMAP.md b/ROADMAP.md
index 65cccb4..a0f8ff5 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -20,7 +20,7 @@ IdealData backtest sonuçları ile **%100 uyumlu** harici backtest + optimizasyo
 | Faz 6 | ✅ | Desktop UI (PySide6) | - |
 | Faz 7 | ✅ | Veritabanı Entegrasyonu | - |
 | Faz 8 | 🔄 | Agent Dokümantasyonu | 🔴 Sürekli |
-| Faz 9 | 🔜 | Canlı Test & S5 Araştırma | 🟡 Düşük |
+| Faz 9 | 🔄 | Canlı Test & S5 Araştırma | 🟡 Düşük |
 
 ---
 
diff --git a/checkpoints/s4_hybrid_VIP_X030-T_1dk_20260222_152338.json b/checkpoints/s4_hybrid_VIP_X030-T_1dk_20260222_152338.json
new file mode 100644
index 0000000..6d1fae5
--- /dev/null
+++ b/checkpoints/s4_hybrid_VIP_X030-T_1dk_20260222_152338.json
@@ -0,0 +1,12 @@
+{
+  "job_id": "s4_hybrid_VIP_X030-T_1dk_20260222_152338",
+  "timestamp": "2026-02-22 16:49:54",
+  "epoch": 1771768194.0788982,
+  "current_phase": 2,
+  "best_phase1": {
+    "toma_period": 3,
+    "toma_opt": 0.9,
+    "hhv1": 420,
+    "llv1": 100
+  }
+}
\ No newline at end of file
diff --git a/presets/VIP_X030-T_1dk_20260222_152338_1dk_S4.json b/presets/VIP_X030-T_1dk_20260222_152338_1dk_S4.json
new file mode 100644
index 0000000..ddc54d4
--- /dev/null
+++ b/presets/VIP_X030-T_1dk_20260222_152338_1dk_S4.json
@@ -0,0 +1,105 @@
+{
+  "symbol": "VIP_X030-T_1dk_20260222_152338",
+  "period": "1 dk",
+  "strategy_index": 3,
+  "strategy_name": "Strateji 4 - TOMA + Momentum",
+  "created": "2026-02-22 16:49:51",
+  "params": {
+    "toma_period": {
+      "min": 1,
+      "max": 3,
+      "step": 1,
+      "active": true
+    },
+    "toma_opt": {
+      "min": 0.1,
+      "max": 3.0,
+      "step": 0.1,
+      "active": true
+    },
+    "hhv1_period": {
+      "min": 10,
+      "max": 600,
+      "step": 10,
+      "active": true
+    },
+    "llv1_period": {
+      "min": 10,
+      "max": 600,
+      "step": 10,
+      "active": true
+    },
+    "mom_period": {
+      "min": 100,
+      "max": 2000,
+      "step": 100,
+      "active": true
+    },
+    "trix_period": {
+      "min": 10,
+      "max": 200,
+      "step": 10,
+      "active": true
+    },
+    "mom_limit_high": {
+      "min": 100.0,
+      "max": 105.0,
+      "step": 0.5,
+      "active": true
+    },
+    "trix_lb1": {
+      "min": 10,
+      "max": 300,
+      "step": 10,
+      "active": true
+    },
+    "hhv2_period": {
+      "min": 10,
+      "max": 600,
+      "step": 10,
+      "active": true
+    },
+    "llv2_period": {
+      "min": 10,
+      "max": 600,
+      "step": 10,
+      "active": true
+    },
+    "mom_limit_low": {
+      "min": 95.0,
+      "max": 100.0,
+      "step": 0.5,
+      "active": true
+    },
+    "trix_lb2": {
+      "min": 10,
+      "max": 300,
+      "step": 10,
+      "active": true
+    },
+    "hhv3_period": {
+      "min": 10,
+      "max": 600,
+      "step": 10,
+      "active": true
+    },
+    "llv3_period": {
+      "min": 10,
+      "max": 600,
+      "step": 10,
+      "active": true
+    },
+    "kar_al": {
+      "min": 0.5,
+      "max": 10.0,
+      "step": 0.5,
+      "active": true
+    },
+    "iz_stop": {
+      "min": 0.5,
+      "max": 5.0,
+      "step": 0.25,
+      "active": true
+    }
+  }
+}
\ No newline at end of file
diff --git a/src/optimization/strategy1_optimizer.py b/src/optimization/strategy1_optimizer.py
index 509d988..1c9ed6d 100644
--- a/src/optimization/strategy1_optimizer.py
+++ b/src/optimization/strategy1_optimizer.py
@@ -40,15 +40,15 @@ class IndicatorCache:
             self.volumes = df['Lot'].values if 'Lot' in df.columns else np.zeros_like(df['Kapanis'].values)
             
         if 'datetime' in df.columns:
-            self.times_arr = df['datetime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['datetime'].astype('datetime64[s]').astype(np.int64).values
         elif 'date' in df.columns:
-            self.times_arr = df['date'].values.astype(np.int64) // 10**9
+            self.times_arr = df['date'].astype('datetime64[s]').astype(np.int64).values
         elif 'Tarih' in df.columns:
-            self.times_arr = df['Tarih'].values.astype(np.int64) // 10**9
+            self.times_arr = df['Tarih'].astype('datetime64[s]').astype(np.int64).values
         elif 'DateTime' in df.columns:
-            self.times_arr = df['DateTime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['DateTime'].astype('datetime64[s]').astype(np.int64).values
         elif 'time' in df.columns:
-            self.times_arr = df['time'].values.astype(np.int64) // 10**9
+            self.times_arr = df['time'].astype('datetime64[s]').astype(np.int64).values
         else:
             self.times_arr = np.zeros(len(df), dtype=np.int64)
 
diff --git a/src/optimization/strategy2_optimizer.py b/src/optimization/strategy2_optimizer.py
index 09c8d2b..38e27ef 100644
--- a/src/optimization/strategy2_optimizer.py
+++ b/src/optimization/strategy2_optimizer.py
@@ -59,15 +59,15 @@ class IndicatorCache:
         self.n = len(self.closes)
         self.lots = df['Lot'].values  # Volume data (Lot)
         if 'datetime' in df.columns:
-            self.times_arr = df['datetime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['datetime'].astype('datetime64[s]').astype(np.int64).values
         elif 'Tarih' in df.columns:
-            self.times_arr = df['Tarih'].values.astype(np.int64) // 10**9
+            self.times_arr = df['Tarih'].astype('datetime64[s]').astype(np.int64).values
         elif 'date' in df.columns:
-            self.times_arr = df['date'].values.astype(np.int64) // 10**9
+            self.times_arr = df['date'].astype('datetime64[s]').astype(np.int64).values
         elif 'DateTime' in df.columns:
-            self.times_arr = df['DateTime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['DateTime'].astype('datetime64[s]').astype(np.int64).values
         elif 'time' in df.columns:
-            self.times_arr = df['time'].values.astype(np.int64) // 10**9
+            self.times_arr = df['time'].astype('datetime64[s]').astype(np.int64).values
         else:
             self.times_arr = np.zeros(len(df), dtype=np.int64)
         
diff --git a/src/optimization/strategy3_optimizer.py b/src/optimization/strategy3_optimizer.py
index 805e5e5..27e66d0 100644
--- a/src/optimization/strategy3_optimizer.py
+++ b/src/optimization/strategy3_optimizer.py
@@ -38,15 +38,15 @@ class IndicatorCache:
             self.volumes = df['Lot'].values
             
         if 'datetime' in df.columns:
-            self.times_arr = df['datetime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['datetime'].astype('datetime64[s]').astype(np.int64).values
         elif 'date' in df.columns:
-            self.times_arr = df['date'].values.astype(np.int64) // 10**9
+            self.times_arr = df['date'].astype('datetime64[s]').astype(np.int64).values
         elif 'Tarih' in df.columns:
-            self.times_arr = df['Tarih'].values.astype(np.int64) // 10**9
+            self.times_arr = df['Tarih'].astype('datetime64[s]').astype(np.int64).values
         elif 'DateTime' in df.columns:
-            self.times_arr = df['DateTime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['DateTime'].astype('datetime64[s]').astype(np.int64).values
         elif 'time' in df.columns:
-            self.times_arr = df['time'].values.astype(np.int64) // 10**9
+            self.times_arr = df['time'].astype('datetime64[s]').astype(np.int64).values
         else:
             self.times_arr = np.zeros(len(df), dtype=np.int64)
         
diff --git a/src/optimization/strategy4_optimizer.py b/src/optimization/strategy4_optimizer.py
index 5c6773b..72872da 100644
--- a/src/optimization/strategy4_optimizer.py
+++ b/src/optimization/strategy4_optimizer.py
@@ -41,15 +41,15 @@ class IndicatorCache:
             raise ValueError("DataFrame must contain 'close' or 'Kapanis' columns")
             
         if 'datetime' in df.columns:
-            self.times_arr = df['datetime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['datetime'].astype('datetime64[s]').astype(np.int64).values
         elif 'date' in df.columns:
-            self.times_arr = df['date'].values.astype(np.int64) // 10**9
+            self.times_arr = df['date'].astype('datetime64[s]').astype(np.int64).values
         elif 'Tarih' in df.columns:
-            self.times_arr = df['Tarih'].values.astype(np.int64) // 10**9
+            self.times_arr = df['Tarih'].astype('datetime64[s]').astype(np.int64).values
         elif 'DateTime' in df.columns:
-            self.times_arr = df['DateTime'].values.astype(np.int64) // 10**9
+            self.times_arr = df['DateTime'].astype('datetime64[s]').astype(np.int64).values
         elif 'time' in df.columns:
-            self.times_arr = df['time'].values.astype(np.int64) // 10**9
+            self.times_arr = df['time'].astype('datetime64[s]').astype(np.int64).values
         else:
             self.times_arr = np.zeros(len(df), dtype=np.int64)
         
diff --git a/test_date2.py b/test_date2.py
new file mode 100644
index 0000000..cc1da6a
--- /dev/null
+++ b/test_date2.py
@@ -0,0 +1,29 @@
+import sys
+import os
+import pandas as pd
+import numpy as np
+
+sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
+
+from src.engine.data import OHLCV
+from src.optimization.strategy4_optimizer import IndicatorCache
+
+csv_path = "d:/Projects/IdealQuant/data/VIP_X030T_1dk_.csv"
+data = OHLCV.from_ideal_export(csv_path)
+
+print("DataFrame Columns:", data.df.columns)
+cache = IndicatorCache(data.df)
+
+print("First 5 times_arr:", cache.times_arr[:5])
+print("First 5 times_arr // 86400:", cache.times_arr[:5] // 86400)
+print("Unique days in total data:", len(np.unique(cache.times_arr // 86400)))
+
+# Assume index 0 to 10000 is train, rest is test
+split_idx = int(len(data.df) * 0.70)
+test_df = data.df.iloc[split_idx:].copy()
+test_cache = IndicatorCache(test_df)
+
+print("\n--- TEST DATA ---")
+print("First 5 test times_arr:", test_cache.times_arr[:5])
+print("First 5 test times_arr // 86400:", test_cache.times_arr[:5] // 86400)
+print("Unique days in test data:", len(np.unique(test_cache.times_arr // 86400)))
diff --git a/test_date3.py b/test_date3.py
new file mode 100644
index 0000000..30a090c
--- /dev/null
+++ b/test_date3.py
@@ -0,0 +1,32 @@
+import sys
+import os
+import pandas as pd
+import numpy as np
+
+sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
+
+from src.engine.data import OHLCV
+
+csv_path = "d:/Projects/IdealQuant/data/VIP_X030T_1dk_.csv"
+data = OHLCV.from_ideal_export(csv_path)
+
+dt_series = data.df['datetime']
+
+print("dt_series dtypes:", dt_series.dtype)
+print("dt_series first val:", dt_series.iloc[0])
+
+arr = dt_series.values
+print("values dtype:", arr.dtype)
+
+arr_int = arr.astype(np.int64)
+print("values as int64:", arr_int[:5])
+
+print("To seconds depending on datatype:")
+if arr.dtype == 'datetime64[ns]':
+    print("It's nanoseconds. Divide by 10**9")
+elif arr.dtype == 'datetime64[s]':
+    print("It's seconds. Leave as is.")
+elif arr.dtype == 'datetime64[ms]':
+    print("It's milliseconds. Divide by 10**3")
+else:
+    print("Unknown dtype")
diff --git a/test_date4.py b/test_date4.py
new file mode 100644
index 0000000..27177a9
--- /dev/null
+++ b/test_date4.py
@@ -0,0 +1,20 @@
+import sys
+import os
+import pandas as pd
+import numpy as np
+
+sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
+
+from src.engine.data import OHLCV
+
+csv_path = "d:/Projects/IdealQuant/data/VIP_X030T_1dk_.csv"
+data = OHLCV.from_ideal_export(csv_path)
+
+dt_series = data.df['datetime']
+seconds_arr = dt_series.astype('datetime64[s]').astype(np.int64).values
+
+print("Using astype('datetime64[s]').astype(int64):")
+print(seconds_arr[:5])
+print("Divided by 86400 (days):")
+print(seconds_arr[:5] // 86400)
+print("Unique days:", len(np.unique(seconds_arr // 86400)))
diff --git a/test_date5.py b/test_date5.py
new file mode 100644
index 0000000..2941ffe
--- /dev/null
+++ b/test_date5.py
@@ -0,0 +1,23 @@
+import pandas as pd
+import numpy as np
+import io
+
+csv_content = """Tarih;Saat;Acilis;Yuksek;Dusuk;Kapanis;Ortalama;Hacim;Lot
+01.01.2024;09:30:00;100;105;95;102;100;1000;1000
+01.01.2024;09:31:00;102;106;101;105;103;2000;2000
+02.01.2024;09:30:00;105;110;104;108;106;1500;1500
+03.01.2024;09:30:00;108;112;105;110;108;1800;1800"""
+
+df = pd.read_csv(io.StringIO(csv_content), sep=';')
+df['DateTime'] = pd.to_datetime(df['Tarih'] + ' ' + df['Saat'], format='%d.%m.%Y %H:%M:%S', errors='coerce')
+
+# Check how IndicatorCache handles it
+if 'DateTime' in df.columns:
+    try:
+        times_arr = df['DateTime'].astype('datetime64[s]').astype(np.int64).values
+        print("times_arr:", times_arr)
+        days = times_arr // 86400
+        print("Days:", days)
+        print("Unique Days:", len(np.unique(days)))
+    except Exception as e:
+        print("Error:", e)
diff --git a/test_date6.py b/test_date6.py
new file mode 100644
index 0000000..589e6d3
--- /dev/null
+++ b/test_date6.py
@@ -0,0 +1,10 @@
+import pandas as pd
+import numpy as np
+
+s = pd.Series(["01.01.2024", "02.01.2024"])
+
+try:
+    arr = s.astype('datetime64[s]').astype(np.int64).values
+    print("Parsed!", arr)
+except Exception as e:
+    print("Exception:", e)
